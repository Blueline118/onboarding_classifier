import { FC } from "react";

const InputsForm: FC = () => {
  return (
    <form>
      {/* TODO: Build classifier input controls */}
      <fieldset>
        <legend>Inputs form placeholder</legend>
      </fieldset>
    </form>
  );
};

export default InputsForm;